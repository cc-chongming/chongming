- 后端代码严格遵循 api→application→domain→infrastructure 四层划分，domain 层不依赖 infrastructure。
- 仓储接口在 domain/repository 定义，由 infrastructure/persistence 与 InMemory* 双实现提供，运行时通过配置切换内存/MySQL 模式。
- AgentScope 适配集中在 infrastructure/agentscope，领域工具通过该层绑定到 Harness，避免领域模型直接耦合框架。
- 前端通过 REST API 与 SSE 与后端交互，SSE 通道遵循 AG-UI 前端对话契约。
- docs/AIREVIEW-PLAN-* 每个文档限定单一子系统边界，完成标记需附带可执行的集成证据而非仅代码变更。