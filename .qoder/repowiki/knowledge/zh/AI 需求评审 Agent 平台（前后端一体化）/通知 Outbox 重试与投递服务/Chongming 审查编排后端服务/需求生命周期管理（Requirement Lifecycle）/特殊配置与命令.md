评审启动接口 `POST /api/requirements/{requirementId}/reviews` 要求携带 `Idempotency-Key` 请求头以实现幂等提交，并通过 multipart/form-data 上传 requirementFile 与多个 form 参数；所有写操作均通过 `expectedVersion` 乐观锁校验，版本不匹配时抛出 `VERSION_CONFLICT`。