- 应用服务以单一用例为粒度拆分，每个 Service 聚焦一个端到端场景（摄入、编排、恢复、查询、快照等），控制器仅做薄包装。
- 跨用例共享状态通过 ReviewRuntimeContext/ReviewContextAssembler 在编排链中传递，避免隐式全局变量。
- 领域模型位于 domain/model，不依赖基础设施；仓储接口定义于 domain/repository，由 infrastructure 提供多实现并通过配置切换。
- AgentScope 相关调用统一经 infrastructure/agentscope 适配层，应用服务不直接耦合框架细节。